name: buygood
version: "1.0.0"

profile: buygood

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - target
  - dbt_packages

models:
  buygood:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
